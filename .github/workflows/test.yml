name: test

on:
  workflow_run:
    workflows: [build]
    types: [completed]
  workflow_dispatch:
  schedule:
    # Also run tests independently on Tuesdays — catches conda-forge regressions
    # even when the image itself hasn't changed
    - cron: '45 4 * * 2'

permissions:
  contents: read
  packages: read

jobs:
  sanity:
    if: >
      github.event_name != 'workflow_run' ||
      github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/hypertidy/conda-gdal-r-python:latest
      options: --user root
    steps:
      - uses: actions/checkout@v4
      # _entrypoint.sh wrapper is micromamba-docker's mechanism for re-activating the base env when
      # Actions has bypassed the normal entrypoint.
      - name: Run sanity tests
        run: |
          export PATH="/opt/conda/bin:$PATH"
          chmod +x local-test.sh
          bash local-test.sh
