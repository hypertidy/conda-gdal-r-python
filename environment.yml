name: base
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python>=3.12
  - r-base>=4.5
  - gdal
  - geos
  - proj
  # R geo
  - r-terra
  - r-sf
  - r-gdalraster
  # R all for vapour which installs from source (for now)
  - r-glue
  - r-lifecycle
  - r-vctrs
  - r-stringr
  - r-stringi
  - r-jsonlite
  - r-nanoarrow
  - r-wk
  - r-cpp11
  # R bridge
  - r-reticulate
  # Python geo
  - rasterio
  - pyogrio
  - fiona
